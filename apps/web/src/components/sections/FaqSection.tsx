import { ArrowUpRight } from "lucide-react";
import { CurrentReveal } from "@/components/motion/CurrentMotion";
import { CurrentLabel,StatusSignal } from "@/components/solar/SolarPrimitives";
import { Accordion,AccordionContent,AccordionItem,AccordionTrigger } from "@/components/ui/accordion";
const faqs=[["What can I do on Solana today?","Silfable supports Jupiter swaps, Pump.fun token launch preparation, portfolio context, reviewed automation proposals, and supported bridge routes."],["How does signing differ between web and desktop?","Web transactions are confirmed in the connected browser wallet. Desktop signs supported transactions through its encrypted local vault."],["What will I see before confirming?","The review includes wallet, network, assets, amount, expected output, route, fees, timing, and venue checks."],["What if route data is incomplete?","Silfable stops the action and identifies missing or stale evidence before any wallet request."],["Is Robinhood Chain still supported?","Yes. It remains a connected EVM network for supported Uniswap swaps and bridge workflows."]] as const;
export function FaqSection(){return <section className="operatorSection"><div className="section-shell faqOperator"><CurrentReveal className="faqIntro"><CurrentLabel>Operator reference</CurrentLabel><h2>Know the boundary before you connect.</h2><p className="mt-7 max-w-sm leading-7 text-[var(--sc-muted)]">Practical answers about routes, approval, and supported surfaces.</p><a href="/docs" className="mt-7 inline-flex items-center gap-2 text-sm text-[var(--sc-ice)]">Read documentation <ArrowUpRight className="size-4"/></a></CurrentReveal><CurrentReveal delay={.08}><div className="mb-5"><StatusSignal>Five notes</StatusSignal></div><Accordion type="single" collapsible className="faqNotes">{faqs.map(([question,answer],index)=><AccordionItem key={question} value={`note-${index}`}><AccordionTrigger className="faqQuestionTrigger py-7 text-lg"><span className="font-mono text-[9px] text-[var(--sc-violet)]">{String(index+1).padStart(2,"0")}</span><span className="faqQuestionText">{question}</span></AccordionTrigger><AccordionContent className="faqAnswer max-w-2xl text-base leading-8 text-[var(--sc-muted)]">{answer}</AccordionContent></AccordionItem>)}</Accordion></CurrentReveal></div></section>}
