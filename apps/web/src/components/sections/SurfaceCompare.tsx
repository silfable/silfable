import { Cloud,HardDrive } from "lucide-react";
import { CurrentReveal } from "@/components/motion/CurrentMotion";
import { CurrentLabel,NetworkIdentity } from "@/components/solar/SolarPrimitives";
const rows=[["Signer","Connected browser wallet","Encrypted local vault"],["Key storage","Never uploaded","OS-backed local storage"],["Records","Account session history","Local encrypted activity"],["Best for","Fast wallet-reviewed actions","Native and long-running workflows"]];
export function SurfaceCompare(){return <section className="operatorSection"><div className="section-shell"><CurrentReveal className="operatorSectionHeader"><div><CurrentLabel>Execution surfaces</CurrentLabel><h2>One control model.<br/>Two signer boundaries.</h2></div><p>Web and desktop prepare the same typed workflows while keeping signing authority in different, explicit locations.</p></CurrentReveal><CurrentReveal className="surfaceMatrix" delay={.08}><div className="matrixHead matrixLabel">Capability</div><div className="matrixHead"><Cloud/><NetworkIdentity>Web</NetworkIdentity><h3>Browser workspace</h3></div><div className="matrixHead"><HardDrive/><NetworkIdentity network="evm">Desktop</NetworkIdentity><h3>Native workspace</h3></div>{rows.flatMap(([label,web,desktop])=>[<div key={`${label}-l`} className="matrixLabel">{label}</div>,<p key={`${label}-w`}>{web}</p>,<p key={`${label}-d`}>{desktop}</p>])}</CurrentReveal></div></section>}
